export const fromBullx = (str: string): string => {
  return str
}
export const toBullx = (str: string): string => {
  return str
}

export const fromAxiom = (str: string): string => {
  return str
}
export const toAxiom = (str: string): string => {
  return str
}

export const fromGmgn = (str: string): string => {
  return str
}
export const toGmgn = (str: string): string => {
  return str
}

export const fromOkx = (str: string): string => {
  return str
}
export const toOkx = (str: string): string => {
  return str
}
